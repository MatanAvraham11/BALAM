OPENAI_API_KEY=
APP_PASSWORD=
# Required for /api/login cookie and Next.js middleware (same value in Vercel env for Python + Node)
APP_SESSION_SECRET=
# Optional: set to 0/false/off to disable Rafael buyer OCR locally (buyer_name becomes "OCR Failed").
# RAFAEL_BUYER_OCR=
