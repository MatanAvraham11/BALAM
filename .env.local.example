OPENAI_API_KEY=
# Used by בל"מ parsing only — not used for Rafael RFQ buyer name (Tesseract only).
APP_PASSWORD=
# Required for /api/login cookie and Next.js middleware (same value in Vercel env for Python + Node)
APP_SESSION_SECRET=
# Rafael V.5.4 buyer OCR: set to 0/false/off to skip Tesseract (API returns buyer_ocr_status=skipped_no_tesseract; buyer_name empty)
# RAFAEL_BUYER_OCR=
# PDF smoke tests: set to 1 to assert exact buyer Hebrew strings (requires Tesseract + heb)
# RAFAEL_STRICT_BUYER=
