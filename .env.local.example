OPENAI_API_KEY=
APP_PASSWORD=
# Required for /api/login cookie and Next.js middleware (same value in Vercel env for Python + Node)
APP_SESSION_SECRET=
# Optional: set to 0 to disable Tesseract OCR for Rafael buyer names (subset PDFs stay empty without it)
# RAFAEL_BUYER_OCR=
